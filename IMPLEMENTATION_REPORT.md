# LumiChat 好友制与隐私改造报告

完成日期：2026-09-05  
改造前安全点：Git commit `938affe`，tag `safety-before-friends-v7`

## 数据库变更

新增表：

- `friendships`：使用排序后的两个用户 ID 保存双向好友关系，联合主键防止重复。
- `friend_requests`：保存发送方、接收方、`pending/accepted/rejected/canceled` 状态和时间。
- `user_blocks`：保存单向拉黑关系；命中任一方向即禁止申请、私聊和呼叫。
- `friend_invites`：保存随机 48 字符 token、所属用户和有效状态。
- `friend_lookup_events`：记录精确查找时间，用于每分钟/小时频率限制。
- `schema_migrations`：记录一次性迁移 `friends_from_legacy_dm_v8`。

已有 `users`、`messages`、`channels`、`sessions` 数据和聊天记录均未删除。首次升级会把历史上已有私聊消息的双方迁移为联系人；迁移标记写入后不会在后续重启时把已删除的好友重新建立。

## 新增 API

- `GET /api/friends`
- `POST /api/friends/lookup`
- `GET|POST /api/friend-requests`
- `POST /api/friend-requests/:id/accept`
- `POST /api/friend-requests/:id/reject`
- `POST /api/friend-requests/:id/cancel`
- `DELETE /api/friends/:id`
- `POST /api/friends/:id`（拉黑）
- `POST /api/friends/:id/unblock`
- `GET /api/friend-invite`
- `POST /api/friend-invite/regenerate`
- `GET /api/users/:id`

## 权限逻辑

- `/api/users` 改为管理员专用；普通用户返回 404。
- 资料接口仅允许本人、管理员或好友访问；其他情况统一返回 404。
- 精确查找只接受完整用户名、纯数字 UID 或有效邀请 token，不提供候选列表。
- 查找接口限制每分钟 10 次、每小时 40 次；自查、未命中、拉黑等场景统一失败。
- 防止添加自己、重复申请、已是好友时重复申请；拒绝或取消后可在频率限制内重新申请。
- 私聊发送、语音/视频 WebSocket 信令均在后端检查好友关系和拉黑状态。
- 删除好友后保留私聊历史，但禁止继续发送；拉黑同时解除好友关系并处理待审申请。
- WebSocket 不发布全站在线列表；好友关系变化只定向通知相关双方。
- 普通消息搜索只覆盖用户可见的频道与自己的私聊；管理员审计接口独立鉴权。

## 前端变更

- 移除普通用户的全体成员目录，侧栏仅显示已接受的联系人。
- 新增“添加好友”和“好友申请”入口，支持收到、发出、接受、拒绝、取消。
- 支持精确用户名/UID 添加、邀请链接复制、二维码展示和 token 重新生成。
- 好友会话增加删除好友与拉黑操作。
- Apple 风格方案 2：单侧栏、宽松阅读区、暖白与森林绿、弱边框和圆角层级。
- 桌面、平板和手机布局响应式适配；窄屏侧栏可抽屉展开，弹窗变为底部面板。
- 保留文字、图片/文件、消息回复/编辑、语音/视频和管理员功能。
- 管理员“全部记录”支持聊天记录分页、搜索、频道/私聊筛选、集中图片集与图片预览。
- 管理员可一键清空全部聊天及对应本地附件，并通过 WebSocket 同步刷新在线客户端。

## 实际测试

自动化好友/隐私回归：29 项通过。

| 场景 | 结果 |
| --- | --- |
| A 默认看不到 B | 通过 |
| A 精确用户名与 UID 查找 B | 通过 |
| A 模糊查找 B | 返回 404，通过 |
| A 发送申请，B 接受 | 通过 |
| A/B 双向出现在联系人列表 | 通过 |
| A/B 文字、图片与历史消息 | 通过 |
| B 添加 C 后 A 仍看不到 C | 通过 |
| 普通用户不能读取全站用户列表 | 通过 |
| A 不能读取非好友 C 的资料 | 通过 |
| 删除好友后保留历史但不能发新消息 | 通过 |
| 邀请 token 生成、更新和旧 token 失效 | 通过 |
| 拉黑后申请与私聊被阻止 | 通过 |
| 好友之间视频信令送达 | 通过 |
| 非好友视频信令被后端阻止 | 通过 |
| 已删除好友在容器重启后不会恢复 | 通过 |
| 历史私聊首次迁移为联系人 | 通过 |
| 生产健康检查、HTTPS 与禁止收录响应头 | 通过 |

格式与静态检查：`cargo fmt --check`、`git diff --check`、`node --check web/app.js` 和三个 Python 测试脚本编译均通过。

## 尚未自动化验证

- 两台真实手机、不同运营商和严格 NAT 条件下的摄像头/麦克风媒体质量。WebRTC 信令权限已自动化验证，真实媒体仍依赖浏览器权限、网络直连或 TURN。
- 大量并发好友申请、万级联系人和大规模聊天库的压力测试。
- 管理员“一键清空”未在生产数据上执行；已通过隔离环境实现和代码路径检查，避免影响现有数据。

## 部署与回滚

- 正式容器：`lumichat`
- 升级前镜像标签：`lumichat:pre-friends-v7`
- 升级前数据备份：由部署者自行指定的安全备份目录
- 生产数据卷：Docker Compose 创建的命名卷
- 生产源码：部署者自行指定的项目目录

回滚前应先停止写入，并同时恢复 v7 镜像和对应数据库/上传备份，避免新旧 schema 与数据状态混用。
