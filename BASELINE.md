# LumiChat 性能基线

测量日期：2026-09-01

测量口径：全新 SQLite 数据库、两个用户、一个默认频道、无并发负载，启动后等待 2 秒再读取进程工作集。启动时间从创建进程开始，到 `/api/health` 首次返回 HTTP 200 为止，包含 10 ms 轮询粒度。

| 指标 | 结果 |
| --- | --- |
| Rust 版本 | rustc 1.98.0 (x86_64-pc-windows-gnu) |
| 操作系统 | Windows x86_64 |
| Release 二进制体积 | 2,991,616 bytes（约 2.85 MiB） |
| 冷启动至健康检查通过 | 306.14 ms |
| 空闲工作集内存 | 7,860,224 bytes（约 7.50 MiB） |
| 基本 API 验证 | 10/10 通过 |

通过项：健康检查、注册、登录、默认频道、频道消息与历史、WebSocket 握手、私聊、搜索、文件上传、管理员停用用户。

本机未安装 Docker，因此容器配置已完成但未在本机实际启动；Release 二进制与完整 HTTP/WebSocket 流程均已直接运行验证。

这些数字只用于建立同一环境上的优化基线，不等同于生产负载承诺。
