# LumiChat 性能基线

测量日期：2026-09-05

测量口径：Linux Docker 隔离实例使用全新 SQLite 数据库，等待健康检查返回 HTTP 200 后读取容器统计；无并发业务负载。启动时间从创建容器到健康检查首次成功，包含容器创建与轮询时间。以下结果用于同环境后续优化，不是高并发容量承诺。

| 指标 | v8 好友制基线 |
| --- | --- |
| 冷启动至健康检查通过 | 1,040 ms |
| 空闲容器内存 | 1.414 MiB |
| 空闲 CPU | 0.00% |
| Linux release 二进制 | 3,595,056 bytes（约 3.43 MiB） |
| Docker 镜像 | 78,518,165 bytes（约 74.88 MiB） |
| Docker 内存上限 | 128 MiB |
| 好友/隐私 API 回归 | 29 项通过 |
| WebSocket 通话权限回归 | 通过 |

正式容器部署后复测：空闲内存约 1.273 MiB，CPU 0.00%。升级前同一正式容器约 1.578 MiB，CPU 0.00%；两次都是瞬时空闲采样，差异不应视为稳定性能提升。

已验证：健康检查、注册/登录、好友精确查找、申请/接受/拒绝/取消、关系不扩散、删除好友、拉黑、邀请 token 更新、私聊文字与图片、历史保留、WebSocket 视频信令权限、数据库迁移与重启持久性。

真实音视频媒体的 CPU 和带宽主要由浏览器端点对点处理；服务端只转发 WebRTC 信令。若配置 TURN，中继服务器的带宽和资源需要单独测量。
